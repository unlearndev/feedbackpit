<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Welcome Email
    |--------------------------------------------------------------------------
    |
    | When enabled, newly registered users are sent a welcome email via a
    | queued job. Disabled by default so it must be explicitly turned on per
    | environment.
    |
    */

    'welcome_email_enabled' => env('WELCOME_EMAIL_ENABLED', false),

];
